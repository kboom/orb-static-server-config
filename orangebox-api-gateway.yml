server:
  port: 5000

zuul:
  ignoredServices: '*'
  routes:
    orangebox-storage: /storage/**
    orangebox-trading: /trading/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
#    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}} # use this for testing multiple instances at a single host (in production it makes no sense though)
    hostname: localhost