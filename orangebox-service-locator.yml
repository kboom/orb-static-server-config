server:
  port: ${host.instance.port:8761}

eureka:
  instance:
    appname: orangebox-service-locator
    hostname: ${host.instance.name:localhost}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/