server:
  port: 5501

eureka:
  instance:
    appname: orangebox-storage
    hostname: localhost
    nonSecurePort: 5501
    metadataMap:
        instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}} 

  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    